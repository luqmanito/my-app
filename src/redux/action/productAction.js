export const setProduct = (payload) => {
    // return { type: "UPDATE_DATA_PRODUCT", payload: result.data.result }
    // return { type: "UPDATE_DATA_PRODUCT", payload: payload }
    return { type: "UPDATE_DATA_PRODUCT", payload}
} 